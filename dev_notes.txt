New Development:
Need to Built FastAPI for PDU (power distribution Unit) - attached Proposed code flow document for start building code - there are only four API used in entire app 1. auth, 2.mqtt config, 3. status (bat+chgr basic info), 4. remote shutoff
lets start very simple FastAPI code - there is no DB , logging now
1. AUTH API: start FastAPI code with authentication API key like below and also attached complete reference code folder for reference and decoder token is "VISICS" as same as reference
def verify_acv_token(token: str = Depends(oauth2_scheme)):
    if token is None:
        raise credentials_exception
    try:
        payload = jwt.decode(token, "visics", algorithms=[ALGORITHM])
        exp = payload.get("exp")
        # Convert expiration time to a timezone-aware datetime object
        exp_time = datetime.fromtimestamp(exp, tz=timezone.utc)
 
        if datetime.now(timezone.utc) > exp_time:
            raise token_expire_exception
        return payload
    except InvalidTokenError:
        raise credentials_exception

refer to smart_control_module for building fastapi for get auth \

2. MQTT Config API:  config API for mqtt - refer to smart_control_module how mqtt api used for config ( for testing there is no username & password)

POST: (for configuring new broker info) - hardcode default broker info - change only if new info comes in POST API request
{
  "broker": "192.168.1.100",
  "port": 1883,
  "broker_username":,
  "broker_password":"    
}

response:
{
  "broker": "192.168.1.100",
  "port": 1883,
  "broker_username":,
  "broker_password":",
  "status": "MQTT Broker configured",
 "battery_topic": "PDU/<hostname>/batteryData",    
 "charger_topic": "PDU/<hostname>/chargerData",
}

3. api/status get all basic information mentioned on proposal pdf on request basis (which is combined & filtered data from battery and charger) - to get this need to run the battery & charger code 
so remove main.py code from both unit and control of both device will be on fastapi main.py code and once FastAPI started running same time start charger and battery script publish directly to  mqtt broker as per topics
and same time filter necessary data for status api data like below and respond as per API request

{
  "hostname": "pdu-bbb-001",
  "mac_address": "XX:XX:XX:XX:XX:XX",
  "timestamp": "2026-08-10T13:20:30Z",
  "battery_percentage": 82,
  "battery_state": "Discharging",
  "system_voltage": 51.8,
  "load_current": 4.2,
  "battery_charging_current": 11,
  "battery_capacity_in_min": 120,
  "time_since_boot_in_sec": 2340   
}

4. use emc_board.py code for remote shutoff when this API call hit - call turn_off_all method in emc_board.py code only with request of username, turn_off command, timestamp as request
for remoteshutoff API request it requires username required 
POST request example
{
    "username":"admin",
    "shutoff":true,
    "timestamp":(autogenerated or request machine utc timestamp)
}
i want to built this fastAPI code very simple level without making it to complicated

reference:
Final code for charger and batter in Charger.zip & Battery.zip 
AUTH and MQTT API reference in smart_control_module_main

targeted device: Beaglebone Black industrial , OS: debian Linux

required 1st auth api and mqtt only 1st 